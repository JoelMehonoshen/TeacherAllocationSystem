@!component('components.head', title="Teaching Allocation Preferences")

<body>
    <div class="content-section">
      <div class="home-background flex-container flex-center img-cover">
        <div class="login-card white-transparent-fill">
          <h1 class="title">Teaching Allocation Preferences</h1>
          <form method="POST" action="{{ route('teachingForm.submit') }}">
            {{ csrfField() }}

            <label for="name">Name:</label>
            <input type="text" id="name" name="name" required>

            <label for="id">ID:</label>
            <input type="text" id="id" name="id" required>
            <br/><br/>
            <h2>Classes:</h2>
            <!-- Load the component allow for selecting preference options -->
            @!component('components.addPreferenceOption', csrf=csrfField(), unitsList=unitsList)

            <button type="button" class="dd-button--round button-blue-fill" data-toggle="modal" data-target="#preferenceList">+ Add Units</button>
            <div class="items">
              @each(item in items)
                <div class="item">
                  <span class="item-name">{{ item.name }}</span>
                  <div class="item-ratings">
                    <span>Experience:</span>
                    <select name="experience[{{ item.name }}]">
                      @for(i in 1..5)
                        <option value="{{ i }}">{{ i }}</option>
                      @endfor
                    </select>
                    <span>Willingness:</span>
                    <select name="willingness[{{ item.name }}]">
                      @for(i in 1..5)
                        <option value="{{ i }}">{{ i }}</option>
                      @endfor
                    </select>
                  </div>
                </div>
              @endeach
            </div>
            <br/>
            <button type="submit"  class="dd-button--round button-blue-fill">Submit</button>
          </form>
        </div>
      </div>
    </div>
    <div class="container">
  </div>
  <!-- @include('partials.footer') Currently, this line is bugged, and does not render -->
</body>
</html>
