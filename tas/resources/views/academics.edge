<!DOCTYPE html>
<html>
  @!component('components.head', title="Teacher Allocation System")
  <body>
    <div class="wrapper">
      @include('partials/sidebar')
      <div class="content-section">
        <div class="main-content">
          @!component('components.addacademic', {
            csrf: csrfField(),
          })
          @!component('components.searchAcademics', {
            title: "Find academic (name | ID)...",
            academics: academics,
            units: units,
            csrf: csrfField(),
            route: route('AcademicController.render'),
          })
          <div>
            {{-- Display Academics --}}
            @!component('components.displayAcademics', {
              csrf: csrfField(), 
              route1: route('AcademicController.render'),
              route2: route('AcademicController.addPreference'),
              route3: route('AcademicController.updatePreference'),
              route4: route('AcademicController.deletePreference'),
              academics: academics,
              groupedPreferences: groupedPreferences,
              units: units,
            })
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
