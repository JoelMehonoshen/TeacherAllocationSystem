<!DOCTYPE html>
<html>
  @!component('components.head', title="Teacher Allocation System")
  @include('partials/sidebar')
  <body class="flex-container">
    <div class="main-content dd-font">
      {{--  Title, Search Bar--}}
      <div class="level">
        <div>
          <h1 class="page-title">Units</h1>
          <h2 class="page-subtitle">Add and Search for Units</h2>
        </div>
              
      @!component('components.addunit', csrf=csrfField())
      </div>

      @!component('components.searchbar', title="Find Unit", units=units, csrf=csrfField(), route=route('UnitController.render'))

      <div>
        {{-- Display Units --}}
        @!component('components.displayUnit', csrf=csrfField(), route=route('UnitController.render'), units=units)
      </div>

    </div>
  </body>
{{--
  <body>
  <section class="section">
    <div class="container">
      <div class="section is-small">
        <div class="level">
          <div class="container">
            <h1 class="title">Units</h1>
            <h2 class="subtitle">Add and Search for Units</h2>
          </div>
          <div class="level is-right">
            @!component('components.addunit', csrf=csrfField())
          </div>
        </div>
      </div>
      <div class="section">
        @!component('components.searchbar', title="Find Unit", units=units, csrf=csrfField(), route=route('UnitController.render'))
      </div>
      @!component('components.displayUnit', csrf=csrfField(), route=route('UnitController.render'), units = units)
      <br>
      <br>
      <br>
      <br>
      <br>
      <br>
    </section>
  </body>
  --}}
</html> 
